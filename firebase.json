{
  "firestore": [
    {
      "database": "dttracker-dev",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    },
    {
      "database": "dttracker-staging",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    },
    {
      "database": "dttracker-prod",
      "rules": "firestore.rules",
      "indexes": "firestore.indexes.json"
    }
  ],
  "database": [
    {
      "target": "dev",
      "rules": "database.rules.json"
    },
    {
      "target": "staging",
      "rules": "database.rules.json"
    },
    {
      "target": "prod",
      "rules": "database.rules.json"
    }
  ],
  "storage": [
    {
      "target": "dev",
      "rules": "storage.rules"
    },
    {
      "target": "staging",
      "rules": "storage.rules"
    },
    {
      "target": "prod",
      "rules": "storage.rules"
    }
  ]
}
